query-1-1.jq
For each library with non-zero offers, print name of the library and number of offers.

query-1.2.jq
Create array of all offers which exist during 3.5.2017.

query-2-1.jq
Create array with objects containing number of books written in a given genre.

query-4-1.jq
Group authors by language they speak, also transforming they names to a single name string.